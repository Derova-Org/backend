# Hedera credentials
HEDERA_OPERATOR_ID=0.0.xxxxxx
HEDERA_OPERATOR_KEY=302e...
HMAC_KEY=your-hmac-key-hex

# PostgreSQL (optional -- omit to use file-backed JSON storage)
DATABASE_URL=postgresql://derova:derova@localhost:5432/derova_dev

# JWT session
JWT_SECRET=your-jwt-secret-here
SESSION_EXPIRY_SECONDS=86400

# CORS
CORS_ORIGINS=http://localhost:5180
